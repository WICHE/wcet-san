{#
/**
 * @file single_text_area — a rich-text block at a readable measure.
 * Field: field_text_area (formatted text).
 */
#}
<section{{ attributes.addClass('section', 'paragraph-text') }}>
  <div class="wrap wrap--narrow">{{ content.field_text_area }}</div>
</section>
